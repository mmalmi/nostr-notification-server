# Keep port for server binding
http_port = 3030

# Add base_url for external URL
base_url = "http://0.0.0.0:3030"
relays = [
    "wss://relay.damus.io",
    "wss://nos.lol",
    "wss://relay.primal.net",
    "wss://temp.iris.to",
    "wss://relay.nostr.band",
    "wss://relay.snort.social",
]
db_map_size = 1073741824  # 1GB in bytes (1024 * 1024 * 1024)
icon_url = "https://iris.to/img/android-chrome-192x192.png"
notification_base_url = "https://iris.to"
social_graph_root_pubkey = "npub1g53mukxnjkcmr94fhryzkqutdz2ukq4ks0gvy5af25rgmwsl4ngq43drvk"
use_social_graph = true
# Optional read-only mount of the crawled graph LMDB directory, e.g. /app/social-graph
# social_graph_snapshot_path = "/app/social-graph"
max_p_tags = 10  # skip p-tag notifications for events with more p tags than this (0 = disabled)
push_rate_limit_burst = 20  # immediate web/mobile push notifications allowed per target
push_rate_limit_refill_seconds = 6  # refill one push token per target every 6 seconds
